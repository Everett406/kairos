# Kairos 项目体检与建议

> 初检：2026-09-20 · 基于 main @ v0.4.0 全量通读（前端构建 + lint 均验证通过）
> 复核：同日傍晚对照 rebuild @ v0.4.1（另一位协作者的大迭代）逐条复查——热键 panic 已修（热键改为运行时注册 + 录制式改键），设置页 / 亮色晨雾 / 开机自启 / 剪贴板全局呼出已在 v0.4.1 落地，相应建议已划掉；其余条目复核后仍然成立。
> 一句话总评：架构干净、纪律严明、完成度高，是我见过的个人项目里文档意识最好的那一档。下面的问题都不致命，但有几个是「真机用户一定会撞到」的。

---

## 一、现有问题（按「用户会不会撞到」排序）

### A. 真 bug，建议下个版本就修

**1. 番茄钟的「今日」用的是 UTC 日期，北京时间 0 点到早 8 点的专注会记到前一天**
`src/features/pomodoro/usePomodoro.ts` 里 `today()` 是 `new Date().toISOString().slice(0, 10)`——toISOString 是 UTC。对 UTC+8 用户，每天凌晨 0:00–8:00 的会话会被记到「昨天」，今日时间带和完成计数在那 8 小时里全是错的。修复一行：

```ts
const d = new Date()
return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`
```

**2. GPU 名字永远显示不出来**
`system.rs` 的 nvidia-smi 查询字段里没有 `name`，`GpuInfo.name` 恒为 `None`；而 `MainCanvas.tsx` 的 GPU 卡副标题走的是 `shortName(gpu?.name)`——永远显示「—」。查询里加上 `name` 字段、填进 GpuInfo 即可，顺手的事。

**3. 没有单实例保护（v0.4.1 复核：仍未装）**
连开两个 Kairos，第二个实例注册全局热键必败。加一个 `tauri-plugin-single-instance` 就解决，顺手还能让第二次双击变成「唤起已有窗口」。

**4. 非管理员模式下磁盘列表是空的**
`system.rs` 里磁盘分组（WMI 三表关联）被整个放进了 `if stats.elevated` 分支。但实际上 `Win32_DiskDrive` 这类查询**不需要管理员权限**——只有 LHM 温度/风扇需要。结果是标准模式下主画布「C 盘空间」永远显示「—」。把磁盘查询挪出提权分支，普通模式也能显示磁盘用量，管理员模式只追加温度/忙碌度。

### B. 隐患类，建议尽早处理

**5. CSP 是 null**
`tauri.conf.json` 里 `"csp": null`。这个 app 存了用户的 QQ 音乐 cookie（`qq_login.json` 明文），又到处发网络请求。Tauri 应用配一条基础 CSP（限制 script-src 'self'、connect-src 白名单几个 API 域名）成本很低，能把 XSS 偷 cookie 这条路直接堵死。

**6. 无 NVIDIA 显卡的机器上，后台线程永远每 10~15 秒尝试拉起一次 nvidia-smi**
`spawn_gpu_watcher` 的循环：spawn 失败 → sleep → 再试，直到天荒地老。开销不大但不优雅——检测到「命令不存在」类的错误就该直接退出线程。

**7. 提权后每 2 秒重建全套 WMI 连接**
`mem_info`（内存规格，一辈子不变）、`disk_groups`（磁盘分区，极少变）这些静态信息跟着 2 秒轮询每次都重查，每次还新建 COM 连接。建议：静态信息查一次缓存起来，动态的（温度/风扇/忙碌度）才跟轮询走。

### C. 小瑕疵，顺手清理

- **UA 串还写着 `Kairos/0.3`**（music.rs / weather.rs 各一处）——「版本四处同步」的纪律漏了这第五处，建议改成读 `env!("CARGO_PKG_VERSION")`，一劳永逸。
- **gsap 是死依赖**（package.json 里挂着，src 里零引用），HANDOFF §12 已列，删的时候记得重生成 pnpm-lock。
- `Cargo.toml` 的 authors 还是 `At210Co60`；声明 MIT 许可但仓库里**没有 LICENSE 文件**；README 又写「个人项目，仅供学习交流」——三处说法不一致，建议统一（个人项目不想开放商用就换个表述，或者干脆补上 MIT LICENSE 文件）。
- `CommandBar.tsx` 里有条 `eslint-disable` 注释，但项目用的是 oxlint——死注释，oxlint 的写法是 `// oxlint-disable-next-line`。
- `charts.tsx` 的 `areaShape` 对单点数据会除零（`(i/(n-1))`），现在调用方都挡住了所以没炸，但组件本身留个 `n < 2` 的兜底更稳。
- `useMonitor.ts` 的 cleanup 里 `stopped = false` 紧跟在赋值后面又马上被 return 的闭包置 true，逻辑能跑但绕，顺手理顺。
- `player.tsx` 里 `ended` 事件绑了两遍（两个 useEffect 各绑一次），功能没错但属于重复代码。

### D. 文档与代码的微小漂移

- HANDOFF §10 说「16 个显式 command + 2 个事件」——数过了，命令数是对的，赞。但 favicon（`public/favicon.svg`）和 `src-tauri/icons/` 全套此前还是 **Tauri 脚手架默认图标**，标题栏的 `.titlebar__logo` 也是个空占位。产品都 v0.4 了，门面不能是毛坯。（本次已一并修好：新 K 字形图标入库 `docs/branding/`，favicon 已接线进 index.html，标题栏 logo 已挂上；`src-tauri/icons/` 的 PNG/ICO 二进制需在开发机本地执行一次 `pnpm tauri icon` 重新生成，步骤见 AGENTS.md「图标收尾」。源 PNG 已修正为透明底，首版曾误带白底。）

---

## 二、下一步发展建议（按性价比排序，不含 roadmap 里已有的）

先说你 roadmap 里原有的：v0.4.0 验收、呼吸边条、~~晨雾亮色~~（v0.4.1 已落地）、~~设置页~~（v0.4.1 已落地，含主题/热键/磁贴/音乐源/自启）。下面是我补充的：

**1. 系统托盘（强烈建议插队，比呼吸边条更基础）**
一个「常驻桌面」的仪表，现在点 X 就是真退出，没有托盘、没有最小化到托盘。对常驻工具来说托盘是命根子：右键菜单（显示/隐藏、退出）、点 X 最小化到托盘而不是退出。Tauri 的 tray API 是现成的，半天工作量，用户体验提升却是质变。

**2. ~~开机自启~~（v0.4.1 已由并发迭代落地：设置页 → 开机自启，reg.exe 写 HKCU Run）** ✅

**3. 新版本提示（不一定要自动更新）**
完整 updater 要签名密钥，个人项目可以退一步：启动时 GET 一下 GitHub latest release 的 tag，比本地版本新就在标题栏显示个小点「有新版 →」，点击跳 release 页。成本极低，解决「发了新版没人知道」。

**4. 数据导出/备份**
用户的番茄钟记录、剪贴板历史、偏好都在 localStorage 和 app_data 的 JSON 里。加一个「导出全部数据为一个 zip / 导入」按钮，重装系统或换电脑时不至于清零。你这种「数据不出本机」的定位，导出功能就是承诺的另一半。

**5. 剪贴板的隐私开关**
剪贴板历史会原样记录从密码管理器里复制的密码。加个「暂停记录」开关 + 可选的「开机前 N 分钟不记录」（密码管理器复制的典型场景），或者在 UI 上给单条记录加「设为私密不保存」。做隐私定位的产品，这个功能是信任分。

**6. 命令条加「算数」**
命令条现在是翻译/专注/音乐/剪贴板。加一个「输入算式直接出结果」（比如输入 `128*36` 或 `100cny to usd`），实现也就几十行，但命令条的「随手用」心智会强很多。Launcher 类产品的留存就靠这种高频微场景。

**不建议做的（帮你省时间）：**
- **别做跨平台**：system.rs 里 WMI / LHM / nvidia-smi 全套绑死 Windows，macOS/Linux 适配是重写量级的投入，收益约等于零。README 也已经明说 Windows 10/11，挺好。
- **别做插件系统/主题市场**：个人项目阶段，把现有六模块做深比摊大饼值钱。
- **别急着做设置页全量**：~~先做「自启 + 热键自定义」两个最高频的~~——v0.4.1 已经一步到位做全了，此条作废。

---

## 三、这个项目还能配什么（已随附 / 可后续）

- ✅ **AGENTS.md**（已入库）：给 AI 协作者的上手指南，架构铁律 + 硬纪律 + 已知坑都浓缩进去了，以后让 AI 改代码先读它，能少解释 80%。顶部还有「并发协作协议」，多 Agent 并行开发时防互相覆盖。
- ✅ **新图标**（SVG 源与接线已入库）：`docs/branding/` 存源文件，favicon 与标题栏 logo 已接线；`src-tauri/icons/` 二进制按 AGENTS.md「图标收尾」在本地生成一次即完成。
- 可选项：`.github/ISSUE_TEMPLATE/`（自用备忘也挺好）、LICENSE 文件、CHANGELOG.md（你们已有 HANDOFF §0 沿革表，也可以不重复造）。
